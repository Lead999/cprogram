#include<stdio.h>
#include<conio.h>
int main()
{
	int n,rev=0,y,r;
	printf("enter a value");
	scanf("%d",&n);
	y=n;
	while(y>0)
	{
		r=y % 10;
		rev=rev*10+r;
		y=y/10;
	}
	printf("the reverse value is %d",rev);
	
	return 0;
}
